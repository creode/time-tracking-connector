{
  "name": "productive-connector",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch": "clasp push --watch",
    "push": "clasp push",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.3.0"
  },
  "dependencies": {
    "@types/google-apps-script": "^1.0.99"
  }
}
