{
  "name": "productive-connector",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch": "cd src && clasp push --watch",
    "push": "cd src && clasp push",
    "deploy": "cd src && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.3.0"
  },
  "dependencies": {
    "@types/google-apps-script": "^1.0.99"
  }
}
